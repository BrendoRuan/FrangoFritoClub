package com.FrangoFrito.FrangoFrito.Service;
/*
import com.FrangoFrito.FrangoFrito.Entity.Imagem;
import com.FrangoFrito.FrangoFrito.Repository.ImagemRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

import java.awt.*;
import java.io.IOException;
import java.util.Optional;

@Service
public class ImagemService {
/*
        @Autowired
        private ImagemRepository imageRepository;

        public Image saveImage(MultipartFile file) throws IOException {
            Imagem imagem = new Imagem();
            imagem.setName(file.getOriginalFilename());
            imagem.setData(file.getBytes());
            imagem.setContentType(file.getContentType());
            return imageRepository.save(imagem);
        }

        public Optional<Image> getImageById(Long id) {
            return imageRepository.findById(id);
        }
    }*/